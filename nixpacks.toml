[phases.setup]
nixPkgs = ['...', 'php84Extensions.gd', 'php84Extensions.redis']

[phases.build]
cmds = [
    'composer install --no-dev --optimize-autoloader --no-interaction',
    'npm install',
    'npm run build',
    'php artisan config:cache',
    'php artisan route:cache',
    'php artisan view:cache'
]

[start]
cmd = 'php artisan migrate --force && php artisan optimize && php artisan serve --host=0.0.0.0 --port=$PORT'
```

#### ب) تحديث `.gitignore`
تأكد إن الملفات دي **مش** موجودة في `.gitignore`:
```
# تأكد إن دول مش موجودين
# /public/build
# /bootstrap/cache
